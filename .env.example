# Опционально. По умолчанию docker-compose уже настроен на Mailpit.
# Письма смотрите на http://localhost:8025 (получатель = email из регистрации).

EMAIL_MODE=smtp
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_TLS=none
SMTP_FROM=deadline-notifier@local.test
