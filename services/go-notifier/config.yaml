database:
  host: postgres
  port: 5432
  user: postgres
  password: mysecretpassword
  dbname: deadline_service_db
  sslmode: disable

worker:
  interval_seconds: 60

# Учебный проект: письма перехватывает Mailpit (http://localhost:8025)
email:
  mode: smtp
  smtp:
    host: mailpit
    port: 1025
    tls: none
    username: ""
    password: ""
    from: deadline-notifier@local.test
